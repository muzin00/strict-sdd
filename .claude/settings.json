{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git -C \"$CLAUDE_PROJECT_DIR\" branch --show-current 2>/dev/null); test \"$branch\" != main || { echo 'On main branch: create a feature branch before editing (git checkout -b <type>/<kebab-desc>).' >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
